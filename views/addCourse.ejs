<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Course</title>
    <style>
        body{
            font-family: Arial, sans-serif;
            background-color: #f0f4f8;
            margin: 0; 
        }
        .container{
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f0f4f8;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 75vh;
        }
        h1 {
            color: #333;
            margin-bottom: 20px;
        }
        form {
            display: flex;
            flex-direction: column;
            width: 400px;
            padding: 20px;
            border: 2px solid #4CAF50;
            border-radius: 8px;
            background-color: white;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }
        label {
            margin: 10px 0 5px;
            font-weight: bold;
        }
        input, textarea {
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
        }
        button {
            background-color: #4CAF50;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s;
        }
        button:hover {
            background-color: #45a049;
        }
        a {
            margin-top: 15px;
            text-decoration: none;
            color: #4CAF50;
            font-weight: bold;
            transition: color 0.3s;
        }
        a:hover {
            color: #388e3c;
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    <div class="container">
    <h1>Create a New Course</h1>
    <form id="createCourseForm" action="/admin/course" method="POST">
        <input type="hidden" id="token" name="token" required>

        <label for="title">Course Title:</label>
        <input type="text" id="title" name="title" required>
        
        <label for="description">Description:</label>
        <textarea id="description" name="description" rows="4" required></textarea>
        
        <label for="imageURL">Image URL:</label>
        <input type="text" id="imageURL" name="imageURL" required>
        
        <label for="price">Price:</label>
        <input type="number" id="price" name="price" required step="0.01">

        <button type="submit">Create Course</button>
    </form>

    <a href="/admin/adminDashboard?adminEmail=<%=admin.email%>" 
        class="back-button">
         Back to Dashboard
     </a>

    <script>
        function addTokenToForm(formId) {
            const token = localStorage.getItem('jwtToken');
            if (token) {
                const hiddenInput = document.getElementById('token');
                if (hiddenInput) {
                    hiddenInput.value = token;
                }
            }
        }

        addTokenToForm('createCourseForm');
    </script>
    </div>
    <%- include('partials/footer') %>
</body>
</html>
