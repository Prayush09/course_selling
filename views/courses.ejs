<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/seeCourses.css">
    <title>Available Courses</title>
    <style>
        /* Make the page use flexbox layout */
        html, body {
            margin: 0;
            padding: 0;
            font-family: 'Arial', sans-serif;
            background-color: #f9f9f9;
            color: #333;
            height: 100%;  /* Ensure body takes full viewport height */
            display: flex;
            flex-direction: column;  /* Layout body as column: header, main, footer */
        }

        header, footer {
            background-color: #333;
            color: #fff;
            text-align: center;
            padding: 1rem;
        }

        main {
            flex-grow: 1;  /* Make main grow to take available space */
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
            text-align: center;
        }

        h1 {
            font-size: 2.5rem;
            margin-bottom: 2rem;
            color: #444;
        }

        .courses {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
        }

        .course {
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 1.5rem;
            width: 300px;
            text-align: left;
            transition: transform 0.3s ease;
        }

        .course h3 {
            font-size: 1.5rem;
            margin-bottom: 0.5rem;
            color: #007bff;
        }

        .course p {
            font-size: 1rem;
            margin-bottom: 1rem;
            color: #555;
        }

        .course p:last-child {
            font-weight: bold;
            font-size: 1.1rem;
        }

        .course:hover {
            transform: translateY(-5px);
        }

        footer {
            background-color: #333;
            color: white;
            text-align: center;
            padding: 20px 0;
            width: 100%;
            font-size: 1rem;
        }

        footer p {
            font-size: 0.9rem;
            margin: 0;
        }
    </style>
</head>
<body>
    <!-- Include Header -->
    <%- include('partials/header') %>

    <!-- Main Content -->
    <main>
        <h1>Explore Our Available Courses</h1>
        <div class="courses">
            <% courses.forEach(course => { %>
                <div class="course">
                    <h3><%= course.title %></h3>
                    <p><strong>Description:</strong> <%= course.description %></p>
                    <p><img src="<%=course.imageURL%>" alt="photo of the course"></p>
                    <p><strong>Price:</strong> $<%= course.price %></p>
                </div>
            <% }); %>
        </div>
    </main>

    <!-- Include Footer -->
    <%- include('partials/footer') %>
</body>
</html>
